def validate_xml_schema(xml_bytes: bytes) -> list[dict]:
    """Stub: validate XML against UBL/CII schema and return issues.
    Returns a list of {rule_id, layer, severity, xpath, message_de}.
    """
    return []

