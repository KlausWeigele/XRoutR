FROM node:20-alpine
WORKDIR /app
COPY package.json pnpm-lock.yaml* tsconfig.json next.config.ts postcss.config.js tailwind.config.ts ./
COPY app ./app
COPY src ./src
RUN npm i -g pnpm && pnpm install --frozen-lockfile || true
EXPOSE 3000
CMD ["pnpm", "dev", "-p", "3000"]

